# Review Journal

The review surface for `notion-ui-viewer-kit` is deliberately narrow: one fixture, one scoring rule, and one local check.

The local checks classify each case as `ship`, `watch`, or `hold`. That gives the project a small review vocabulary that matches its frontend apps focus without claiming live deployment or external usage.

## Cases

- `baseline`: `view drift`, score 211, lane `ship`
- `stress`: `state pressure`, score 168, lane `ship`
- `edge`: `layout risk`, score 201, lane `ship`
- `recovery`: `interaction cost`, score 247, lane `ship`
- `stale`: `view drift`, score 198, lane `ship`

## Note

The repository should be understandable without pretending it is larger than it is.
